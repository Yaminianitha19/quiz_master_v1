1) what is the use of render template?
    - It allows you to include HTML templates and dynamically render them with data.

2) what are the http methods?
    - GET, POST, PUT, DELETE, PATCH

3) default http method?
    - get